<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/navy_blue">

    <!-- App Title -->
    <TextView
        android:id="@+id/appTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Econnect12"
        android:textSize="28sp"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        android:fontFamily="@font/poppins_bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="48dp"/>

    <!-- White Background Rectangle -->
    <View
        android:id="@+id/top_rectangle_start"
        android:layout_width="407dp"
        android:layout_height="613dp"
        android:background="@drawable/white_rectangle__1_"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_percent="0.75"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Content Container -->
    <LinearLayout
        android:id="@+id/whiteContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="24dp"
        android:background="@android:color/transparent"
        app:layout_constraintTop_toBottomOf="@id/appTitle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Lottie Animation -->
        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/lottieAnim"
            android:layout_width="250dp"
            android:layout_height="250dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:contentDescription="Econnect animation"
            app:lottie_autoPlay="true"
            app:lottie_fileName="econnect_loader.json"
            app:lottie_loop="true"
            app:lottie_speed="1" />

        <!-- Tagline -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Stay Connected, Stay Informed, Care Always"
            android:textColor="@color/navy_blue"
            android:textSize="14sp"
            android:textStyle="italic"
            android:layout_marginTop="4dp"/>

        <!-- Description -->

        <!-- Log In Button -->

        <!-- Divider -->
        <TextView
            android:id="@+id/description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="10dp"
            android:gravity="center"
            android:lineSpacingExtra="6dp"
            android:text="Econnect12 is a seamless platform connecting parents, teachers, and students. Track attendance, access notes, view results, and manage everything—effortlessly, all in one place."
            android:textColor="@color/navy_blue"
            android:textSize="15sp" />

        <Button
            android:id="@+id/loginButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:backgroundTint="@color/navy_blue"
            android:elevation="8dp"
            android:fontFamily="@font/poppins_medium"
            android:paddingStart="40dp"
            android:paddingTop="12dp"
            android:paddingEnd="40dp"
            android:paddingBottom="12dp"
            android:text="Log In"
            android:textAllCaps="false"
            android:textColor="@color/light_green_white"
            android:textSize="16sp" />

        <View
            android:layout_width="60dp"
            android:layout_height="1dp"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="8dp"
            android:background="@color/navy_blue" />

        <!-- Help Info -->
        <TextView
            android:id="@+id/helpText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:paddingBottom="16dp"
            android:text="Need help? Mail us at group37@gmail.com\nor contact ‪+91 9184472502‬"
            android:textColor="@color/navy_blue"
            android:textSize="13sp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
